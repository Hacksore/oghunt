# oghunt

The final page for oghunt.

Copy `.env.example` to `.env` and add the REST URL and standard token from an Upstash Redis
database.

```sh
pnpm install
pnpm dev
```

Run `pnpm build` to create the production build.
